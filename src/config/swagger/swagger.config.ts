import { SwaggerOptions } from 'src/common/interface/Swagger-options.interface';

export const swaggerConfig: SwaggerOptions = {
  title: 'Shopping online Application',
  description: 'docs APIs shopping service',
  version: '1.0',
};
